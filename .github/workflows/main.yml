name: Auto Deploy to Cloud Run

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Build and Deploy to Cloud Run
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          export_default_credentials: true

      - name: Build Docker image
        run: |
          gcloud builds submit --tag gcr.io/${{ secrets.GCP_PROJECT_ID }}/nextplot-linebot .

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy nextplot-linebot \
            --image gcr.io/${{ secrets.GCP_PROJECT_ID }}/nextplot-linebot \
            --region ${{ secrets.GCP_REGION }} \
            --platform managed \
            --allow-unauthenticated \
            --memory=512Mi \
            --set-env-vars "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }},LINE_TOKEN=${{ secrets.LINE_TOKEN }}"
